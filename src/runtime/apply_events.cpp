#include "apply_events.h"

namespace sim {
    bool runtime_apply_events(Model& m, Data& d, const Command* cmds, size_t count) {
        (void) m;
        (void) d;
        (void) cmds;
        (void) count;
        return true;
    }
}
